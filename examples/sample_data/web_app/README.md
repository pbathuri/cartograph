# Sample web app

A tiny sample project to demonstrate Cartograph's field inference (this one infers `web_frontend`).

It uses React components with accessible markup (ARIA roles), handles loading/empty/error states,
and budgets Core Web Vitals. Replace `examples/` with your own repos and notes — point `carto ingest`
at any folder and your graph builds itself.
